# Worker-only local variables. Keep real values in .dev.vars.
# Application backend secrets belong in Convex, not the Worker.
WEB_PUSH_VAPID_PRIVATE_KEY=

# Non-secret Worker configuration.
WEB_PUSH_VAPID_PUBLIC_KEY=
WEB_PUSH_SUBJECT=
