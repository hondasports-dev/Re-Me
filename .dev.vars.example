# Worker-only local variables. Keep real values in .dev.vars.
# Application backend secrets belong in the Worker and stay out of the browser.
AUTH0_DOMAIN=
AUTH0_AUDIENCE=
CAPABILITY_SECRET=
VAPID_PRIVATE_KEY=

# Non-secret Worker configuration.
VAPID_PUBLIC_KEY=
VAPID_SUBJECT=
