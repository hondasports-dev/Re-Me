# Browser-visible Preview configuration. Copy this file to `.env.preview.local`.
CLOUDFLARE_ENV=preview
VITE_AUTH0_DOMAIN=
VITE_AUTH0_CLIENT_ID=
# Set the shared Preview deployment URL explicitly; the deploy does not inject it.
VITE_CONVEX_URL=
