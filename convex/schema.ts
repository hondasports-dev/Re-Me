import { defineSchema } from 'convex/server'

// Domain tables land in the schema / authorization issue. This file exists so the
// Convex developer deployment can push a valid empty schema during foundation setup.
export default defineSchema({})
